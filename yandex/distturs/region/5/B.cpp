#include <bits/stdc++.h>
using namespace std;
#define int int64_t
void solve() {
    int m;
    cin >> m;
    int answer = 2;
    for (int n = 38; n >= 3; --n) {
        for (int x = 2; x <= (int)sqrt(sqrt(m)) + 5; ++x) {}
    }
}
signed main() {
    cin.tie(nullptr)->sync_with_stdio(false);
    int t;
    cin >> t;
    while (t--) solve();
}